package com.goodanser.osmglass.phone.routing

/** Travel mode for routing. Mapped to an OsmAnd profile in `OsmAndRouter.osmandProfile`. */
enum class NavigationMode {
    CYCLING,
    WALKING,
    DRIVING,
}
